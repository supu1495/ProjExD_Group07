# GameOver画面を3秒間表示し続ける
            # if lose_screen is not None:
            #     lose_screen.draw(screen)
            # pg.display.update()
            # # 3秒経過したらループを抜ける
            # if pg.time.get_ticks() - gameover_start > 3000:
            #     break